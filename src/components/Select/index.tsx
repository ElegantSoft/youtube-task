import React from "react";

interface Props {
  label: string;
  choices: Array<string>;
}
const Select: React.FC<Props> = () => {
  return (
    <div>
      <h1>selece</h1>
    </div>
  );
};

export default Select;
